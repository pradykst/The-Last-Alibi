# Live AgentKit Evidence

`world-agentkit-live.json` is created only by a successful live evidence run.
It contains redacted commitments and accepted/denied status only. It must never
contain an agent credential, signed message, wallet address, raw nonce, or
resolved human identifier.
